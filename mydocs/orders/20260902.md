# 2026-09-02

## PR #6579 - PR 검토 판정어 정본화

- 완료: code candidate `ea705108e`의 초기 CI가 성공 또는 문서-only skipped로 종료됐다.
- 완료: `edwardkim` reviewer 요청을 등록했다.
- 완료: `mydocs/pr/archives/pr_6579_review.md`를 trailing review 기록으로 추가했다.
- 다음: 이 trailing commit의 최신 head CI와 mergeability를 확인한다.
- 조건 충족 뒤: #6579을 병합하고 devel 반영, 문서 archive, issue/PR 상태, branch 정리를 포함한 merge 후속 처리를 수행한다.

## PR #6578 - 빈 줄 TAC Picture 상단 배치 (`jeong-sik`)

- 완료: contributor head `c1f3c7bf1`에 메인터너 보정 commit `53282aa5e`를 rebase 후 반영했다. 기준은 `upstream/devel` `89db03103`이다.
- 완료: 동일 TAC Picture baseline 경로의 객체 상자 높이 보정과 대상 그림 유일성 단언을 반영했다. full nextest, Native Skia, WASM, Rust lint 검증은 동일 rebase head에서 통과했다.
- 판정: 원본 PR 변경 범위 기준으로 `메인터너 보정 됨 수용 가능`이다. 페이지 5의 캡션·표 흐름 시각 차이는 이 PR과 분리된 renderer 과제로 관리한다.
- 완료: 문서 trailing head `556653609`의 CI까지 green을 확인하고, [#6578](https://github.com/edwardkim/rhwp/pull/6578)을 [merge commit `59bde68e`](https://github.com/edwardkim/rhwp/commit/59bde68e66e3dfe3fdf9b5bb9a7daa311e7c73e7)로 병합했다.
- 완료: `upstream/devel`에 merge SHA 포함을 확인했다. [#6575](https://github.com/edwardkim/rhwp/issues/6575)는 closing keyword로 자동 종료됐다.
- 다음: archive review·오늘할일 운영 기록을 devel에 반영한 뒤, review 문서의 계획대로 maintainer issue/PR comment와 local review branch 정리를 완료한다.

## planet6897 PR #6572, #6576, #6580 통합 후보 검토

- 기준: `upstream/devel` `ea045abff28468722084f5cde9df047a9887453e` 위 `review/planet6897-6572-6576-20260902`.
- provenance-preserving cherry-pick: #6572 `cdf358d75`, #6576 `098eda8ff`, #6580 `e86f64bf5`.
- 메인터너 보정: #6576 IR baseline canonical 정렬 `77618ecf4`; #6580 실제 Hancom 좌표와 단 상단 반례를 고정한 `b63ab2cd6`.
- 검증: full nextest `8923 passed, 46 skipped`, Native Skia, IR/overflow baseline, native/WASM clippy, workspace build, manual `rhwp-wasm-build`, 한글 PDF 기반 visual sweep을 완료했다.
- 판정: #6572 승인; #6576 및 #6580 메인터너 보정 후 수용 가능. 원 PR은 직접 병합하지 않고, 별도 승인 뒤 통합 PR로만 수용한다. #6580 본문의 code-span `#6551` 문구는 closing keyword가 아니므로 issue close는 merge 후 검증 완료 시점까지 보류한다.

## #6243 post-release canary 수정 계획

- #6243 구현은 PR #6297로 `devel`에 병합됐지만 `main` trusted controller 활성화와 실제 canary가 남아
  이슈는 OPEN 상태다.
- 종전 후행 canary #6215는 PR #6473으로 `devel`에 먼저 병합되고 2026-08-30 종료되어, v0.8.6 이후
  canary PR로 사용할 수 없다. #5949 Release Binary dry-run도 Render Diff 비대상이므로 대체 증적이 아니다.
- 최신 `upstream/devel@8eab87ce1`에서 `task_m100_6243_canary`를 만들고
  `mydocs/plans/task_m100_6243.md`의 Stage O2-5를 실제 post-release PR 선택 절차로 수정했다.
- 실제 Render Diff 대상 code candidate가 성공한 뒤 현재 base 전진분을 한 번 병합하고 `mydocs/`
  review-only tail을 추가하는 세 SHA 계보가 없으면 #6243을 닫지 않는다. 의미 없는 no-op canary는 만들지 않는다.
- 메인테이너 요구에 따라 v0.8.6 릴리즈 준비의 필수 게이트를 기여자 목록,
  `CHANGELOG.md`·`CHANGELOG_EN.md`, GitHub 릴리즈 노트 초안으로 고정했다. GitHub PR author와 Git author를
  교차 대사해 cherry-pick 기여자를 누락하지 않고, 세 산출물의 기여자 집합이 일치해야 한다.
- 수정 수행계획을 메인테이너가 승인했고 commit `9601c121c`로 원격 branch에 push한 뒤 PR #6583을 열었다.
  정확한 code head의 CI·CodeQL·Proptest·Adapter는 문서-only fast-pass로 성공했다.
- self-review에서 기존 계획의 과거 Python 계약 수 `18/18`이 현행 기준처럼 남은 점을 발견했다. 현재
  `scripts.tests.test_review_only_fast_pass_workflows` 24/24 통과를 재확인하고 역사값과 현행값을 구분했다.
- 현재 상태: PR #6583 self-review 승인. review 기록 trailing commit·push와 최신 head CI 재확인은 별도 승인 대기.

## #6584 v0.8.6 릴리스 준비

- PR #6583을 merge commit `063041a2c`로 `devel`에 병합하고 post-merge Proptest·Adapter 성공과 #6243 OPEN
  유지를 확인했다. 로컬 `devel`을 동기화하고 병합 완료 local·remote branch를 정리했다.
- 릴리스 준비 중복 이슈가 없음을 검색한 뒤 #6584를 등록하고 `edwardkim` assignee, `packaging` label,
  `v1.0.0` milestone을 지정했다.
- 메인테이너가 지정한 최신 `upstream/devel@063041a2c`를 release base로 고정하고 `task_m100_6584`를 만들었다.
  열린 Draft PR #5953, #6458, #6467은 자동 포함하지 않는다.
- 현재 `main..devel`은 2,214커밋·17,373파일 차이다. 기여자 누락을 막기 위해 Git author·co-author,
  GitHub PR author, cherry-pick·통합 review 기록을 교차 대사하는 결정론적 contributor ledger와
  `handle -> PR` 근거표를 선행 Stage R1로 계획했다.
- 최초 계획은 v0.8.5였으나 메인테이너 결정으로 v0.8.6으로 변경했다. 저장소에서
  `npm/editor/package.json`만 이미 개발 버전 0.8.5이므로 모든 공식 표면을 0.8.6으로 단조 증가시킨다.
  npm 공개 버전은 core·editor 모두 0.8.4이고 두 package의 0.8.6은 미게시 상태다.
- `mydocs/plans/task_m100_6584.md`에 release freeze, 기여자·한/영 CHANGELOG·릴리스 노트 집합 게이트,
  Rust·Docker WASM·CDP E2E·5플랫폼 dry-run, `devel -> main`, 공식 채널과 #5949/#6243 후속 순서를 작성했다.
- 완료: 마스터 수행계획을 승인받아 commit `d5510af1d`로 고정하고 Stage R1에 진입했다. 원격 push는 하지
  않았다.
- Stage R1: `v0.8.4..063041a2c` 2,214커밋에서 Git 정체성 33개, 번호 후보 1,906개를 계측했다. GitHub
  객체 대사 결과 Issue 847개, PR 후보 1,059개이며 그중 merge commit이 범위에 들어온 PR은 253개다. 단순
  언급 806개는 기여 근거에서 제외했다.
- Stage R1: `.mailmap`의 `planet6897`·`jangster77` 누락 alias를 보강하고, 사람 20명·dependabot bot 1개·
  AI 공동작성 disposition 1개로 정규화했다. 미해결 번호와 Git 정체성은 모두 0이다.
- Stage R1: contributor audit 도구와 fixture test 10건, 해시 기반 override, 기계 ledger와 사람용 보고서를
  작성했다. 동일 입력 2회 ledger가 byte-for-byte 일치했으며 ledger SHA-256은
  `934a96927831ec87d2b19db296ea1976111b1f1e815c66291369e2a0c1929c28`다.
- Stage R2 진입 교차 검사에서 실제 `devel` merge PR은 261개인데 번호 후보 기반 원장에는 253개만 잡힌 것을
  발견했다. 번호 없는 merge/squash PR 9개를 독립 조회로 복원하고, 범위 안 task-branch PR 1개를 합쳐 최종
  고유 PR provenance를 262개로 정정했다. 사람 집합 20명과 집합 hash는 바뀌지 않았다.
- 현재 상태: Stage R1 정정 검증·commit 뒤 Stage R2 변경 inventory로 재진입한다. push와 버전 변경은 수행하지
  않았다.
- Stage R1 정정은 commit `3a944a313`으로 고정했다. push는 수행하지 않았다.
- Stage R2: 2,214커밋·17,373파일·262개 PR provenance를 parser/save, layout/render, editing/studio,
  CLI/agent, security, performance, packaging/operations 다중 축으로 분류했다. 모든 PR이 적어도 한 축에
  disposition됐다.
- `CHANGELOG.md` `Unreleased`의 exact font instance API와 HWPX OLE 보존은 release base 포함 SHA와 CLOSED
  이슈를 확인했다. 두 항목은 v0.8.6으로 이동하며, 비어 있는 영문 절과 누락된 주요 사용자 영향 변경을 Stage
  R3에서 함께 보강한다.
- #5949 Linux AArch64 실제 asset, #6243 post-release canary, #6584 릴리스 정산은 OPEN으로 유지한다.
  Draft #5953, #6458, #6467은 비범위다.
- 현재 상태: Stage R2 결과와 local commit을 승인받았다. 인벤토리·오늘할일을 같은 stage commit으로 고정한
  뒤 Stage R3 버전·CHANGELOG·릴리스 노트 편집으로 진입한다. R4 실행은 아직 수행하지 않았다.
- Stage R2 결과를 commit `237e7efe5`로 고정하고 Stage R3에 진입했다.
- Stage R3: Cargo, npm editor, Studio, VS Code, Chrome/Edge, Firefox, Safari와 lock root의 공식
  버전을 0.8.6으로 맞췄다. 파생 `pkg/package.json`, package-lock의 `modern-tar` 0.8.4,
  과거 fixture·보고서 버전 증적은 변경하지 않았다.
- Stage R3: 한·영 CHANGELOG, GitHub Release note 초안, VS Code changelog, 한·영 README,
  third-party license 기준 버전을 현행화했다. Chrome 한·영, Edge, Firefox AMO의
  0.8.6 스토어 문서도 확장 동작 변경과 권한 무변경 근거를 반영해 추가했다.
- Stage R3: 세 공개 릴리스 기록의 사람 20명 credit-key 목록을 ledger와 순서·
  대소문자까지 동일하게 고정하고 bot을 제외하는 test를 추가했다. 기여자 목록 hash는
  `169db39bb034abca43b16bae1e6a9d65f127af0e29785e4dcee855e1bed3a2bf`다.
- Stage R3 검증: release 정책·contributor 19 tests, JSON 파싱, locked Cargo metadata, 변경
  Markdown 링크, `git diff --check`가 모두 통과했다.
- 현재 상태: Stage R3 결과·local commit 승인. Stage R4 장시간 Rust·Docker WASM·
  Studio/CDP·확장 검증은 예상 소요시간·디스크 예산 보고와 별도 승인 전에 실행하지 않는다.
- Stage R4: 필수 Rust lint, release build, full nextest 8,925 PASS/0 fail, Native Skia,
  Docker WASM, npm package, Studio 1,362 PASS/0 fail, Chrome·Firefox·VS Code 빌드와
  CDP E2E를 완료했다. 호스트 responsive 최소 폭 제약은 정본 headless 1,082/1,082
  통과로 분리했고, 삭제 API를 쓰는 과거 TAC 수동 스크립트는 현행 TAC 18/18 통과와
  함께 정책 skip 사유를 기록했다.
- Stage R4 archive 검사: 실제 token·개인키·비공개 폰트·불필요한 빌드 경로 유입 0건,
  font distribution contract 6/6 PASS. 단, `THIRD_PARTY_LICENSES.md`의 Studio·VS Code
  `@noble/hashes`/`canvaskit-wasm` 버전이 lock보다 이전임을 찾아 2.4.0/0.42.0으로
  정정했다.
- Stage R4 재검증: 정정·보고, credential-shaped fixture 제거와 Chrome/Firefox/npm README
  현행화 commit `047353023`에서 AMO source zip을 정본 명령으로 재생성했다.
  2,436 entry, 33,863,021 bytes, SHA-256
  `6f3e878abfd5ad65eb678152f4e33ac765cd9f41a1979816d3fb54c42acbad4f`이며 integrity,
  금지 경로, symlink 내부 타겟, 폰트·라이선스와 entry별 비밀정보 검사가 통과했다.
  검사기에 걸리던 가짜 GitHub token literal은 런타임 조합으로 바꾸고 메인테이너 호스트
  경로를 익명화했다. 민감값 거부 계약은 그대로 유지되며 focused suite 12/12가 통과했다.
- `npm/README.md`는 HWP/HWPX·v0.8.6 core 사용자 설명과 root `CHANGELOG.md` 절대 링크를
  추가했고, `@rhwp/core@0.8.6` dry-pack 7 files에 동일 README가 포함됨을 확인했다.
- Stage R5: PR #6585 exact code head `4280831d1`의 PR-triggered check는 29 success·3 conditional
  skip·0 failure/pending으로 완료됐다. 최신 `upstream/devel@063041a2c`은 release base와 같고
  merge-tree는 충돌 없이 생성됐으며 PR은 작성 시점 `MERGEABLE / CLEAN`이다.
- Stage R5: [Release Binary run #33569503350](https://github.com/edwardkim/rhwp/actions/runs/33569503350)을
  exact head·`tag=test`로 실행해 Windows x86_64, Linux x86_64/AArch64, macOS x86_64/AArch64
  build 5/5 성공과 release job의 의도된 skip을 확인했다. Linux AArch64는 GitHub 표준
  `ubuntu-24.04-arm`에서 native `rhwp --version`이 `rhwp v0.8.6`을 출력했다.
- 다섯 payload archive를 내려받아 4파일 구조, 실행 형식·권한과 SHA-256을 검사했다. 검사 뒤 임시
  다운로드 디렉터리를 삭제했으며 저장소에는 산출물을 추가하지 않았다. 상세 근거는
  [Stage R5 결과](../working/task_m100_6584_stage_r5.md)에 남겼다.
- self-review에서 버전·기여자 집합·CHANGELOG·release note·패키징·보안 계약을 다시 대사했고 차단
  사항을 발견하지 못했다. 판정은 [PR #6585 검토 기록](../pr/archives/pr_6585_review.md)에 `승인`으로
  남겼다.
- 현재 상태: self-review·Stage R5·오늘할일 trailing 문서의 로컬 검증을 완료했다. 이 기록을 같은 source
  branch의 trailing commit으로 고정한다. 원격 push 뒤 latest head의 review-only CI·mergeability 확인과
  PR merge는 각각 별도 승인 게이트다.
- PR #6585를 일반 merge commit `a5f3bf695`로 `devel`에 병합했다. merge push의 CI·CodeQL·Proptest·
  Adapter와 issue-state controller가 모두 성공했고 #6584는 계획대로 OPEN이다.
- 로컬 `devel`을 `upstream/devel@a5f3bf695`로 fast-forward하고 병합 완료 local
  `task_m100_6584` branch를 제거했다. 원격 task branch와 기존 다른 PR review worktree는 별도 소유·
  승인 경계이므로 유지했다.
- Stage R6 사전 대사: `upstream/main@496333b27`은 `upstream/devel@a5f3bf695`의 조상이며,
  merge-tree `3274bc442cb923e14b9e6de248950f04c0d54bff`는 devel tree와 정확히 같았다. 열린 main 대상
  PR, 원격 v0.8.6 tag·Release, npm core/editor 0.8.6은 모두 없었다.
- 현재 `main..devel`은 release-prep 15개 commit을 포함해 2,229개다. 공개 CHANGELOG·release note의
  “v0.8.4 이후 2,214개” 문구가 최종 범위 전체로 읽히는 불일치를 발견했다. 2,214개는 기여자·기능을
  계측한 고정 feature baseline임을 명시하고 이후 버전·검증·릴리스 기록 commit과 분리하는 최소 정정을
  Stage R6 선행 fast-pass PR로 반영한 뒤 `devel -> main` release PR을 연다.
- Stage R6 선행 정정을 commit `512f2d984`로 고정하고 PR #6588을 열었다. exact code head의 CI·CodeQL·
  Proptest·Adapter는 14 success·15 conditional skip·0 failure/pending으로 완료됐고 PR은 작성 시점
  `MERGEABLE / CLEAN`이다. root CHANGELOG가 review-only 허용 경로가 아니므로 Proptest·Adapter worker도
  실제 실행되어 성공했다.
- self-review에서 `v0.8.4..063041a2c`의 2,214 commits와 현재 `upstream/main..code head`의 2,230 commits를
  독립 재계산했다. 한·영 CHANGELOG와 release note의 새 문구는 262개 PR provenance를 전자의 고정 기능
  기준선에 한정하고 이후 버전·검증·릴리스 기록 commit과 분리하므로 실제 계측 경계와 일치한다.
- 차단 발견 사항 없이 [PR #6588 검토 기록](../pr/archives/pr_6588_review.md)을 `승인`으로 남겼다.
  review·오늘할일 trailing 변경은 `mydocs/`만 포함한다. 로컬 commit 뒤 원격 push, latest-head CI 확인과
  merge는 각각 별도 승인 게이트다.
- PR #6588을 일반 merge commit `bd8cdd0a1`로 `devel`에 병합했다. exact merge SHA의 Proptest·Adapter와
  issue-state controller가 성공했고 #6584는 OPEN으로 유지됐다. 로컬 `devel`을 동기화하고 병합 완료
  local task branch만 삭제했으며 remote branch와 기존 review worktree는 보존했다.
- Stage R6 최종 대사에서 `upstream/main@496333b27`이 `upstream/devel@bd8cdd0a1`의 조상이고 merge-tree가
  devel tree `7075796cc33b415cd41054d816631a2d63b02400`와 같음을 확인했다. 실제 범위는 2,232 commits이며,
  열린 main 대상 PR과 v0.8.6 tag·Release는 없었다.
- `devel -> main` release PR #6592를 열었다. 최초 Full CI Lint는 현행 Rust test manifest 정책 도입 전
  `main`을 증분 기준으로 사용해, devel에서 이미 검증·병합된 최상위 integration source 33개를 신규
  위반으로 오판했다. main에는 unit-tier 정책도 없어 같은 기준으로는 후속 검사도 통과할 수 없다.
- 메인테이너가 Stage R6 CI blocker 수정 계획을 승인했다. same-repository `devel -> main`, base ancestor,
  synthetic merge tree와 head tree 동일 조건을 모두 만족할 때만 증분 기준을 exact devel head로 바꾸고,
  일반 PR의 base 비교와 현행 manifest·unit-tier 전체 검사는 유지한다. 전용 branch
  `task_m100_6584_r6_ci_gate`에서 correction PR을 준비한다.
- correction code head `fa194b820`은 workflow와 manifest 계약을 보강했다. YAML·Bash syntax, manifest
  21/21, unit-tier 12/12, CI 영향도 33/33, 문서 링크와 diff 검사가 통과했다.
- exact promotion 격리 재현에서 synthetic merge tree와 head tree가 일치했고, integration 1,110 sources·
  48/48 targets·nextest 최소 6,559 cases 및 unit-tier 4,221 tests/299 modules가 모두 통과했다. fork의
  동명 devel과 일반 feature branch는 실제 base를 유지하고 tree 불일치는 fail-closed 함을 확인했다.
- [Stage R6 CI gate 결과](../working/task_m100_6584_stage_r6_ci_gate.md)에 원인·불변식·검증을 고정했다.
  다음은 이 trailing 결과를 local commit한 뒤 별도 승인을 받아 correction branch push와 devel 대상 PR을
  진행하는 것이다. PR #6592는 수정 PR 병합과 새 exact head의 Full CI 전까지 OPEN·blocked로 유지한다.
- correction PR #6594를 exact head `65cb562c1`로 열었다. Full CI·CodeQL·Proptest·Adapter는
  26 success·3 conditional skip·0 failure/cancelled/pending으로 완료됐고, 기존 실패 지점인 manifest
  정책 검사와 후속 unit-tier·Rust lint·archive가 실제 GitHub runner에서 통과했다.
- self-review에서 일반 PR base 유지, same-repository `devel -> main`, ancestry와 synthetic tree guard,
  fail-closed 반례 및 live `Build & Test` required context를 다시 대사했다. manifest 21/21, unit-tier
  12/12, CI 영향도 33/33과 YAML·diff 검사가 통과했고 차단 사항을 발견하지 못했다.
- [PR #6594 검토 기록](../pr/archives/pr_6594_review.md)의 최종 판정은 `승인`이다. 후행 review·오늘할일은
  `mydocs/`만 바꾸며, 별도 승인 뒤 local commit과 remote push를 진행한다. latest-head CI·mergeability와
  merge 승인은 독립 게이트다.
- PR #6594를 정상 merge commit `7dcf162be`로 `devel`에 병합했다. 갱신된 release PR #6592 exact
  code head에서 canonical promotion manifest와 전체 CI·CodeQL·Proptest·Adapter·Skill router가 모두
  성공했고, PR check 집계는 56 success·5 policy skip·0 failure/pending이었다.
- release promotion 범위를 현재 code head 기준 2,236 commits·17,404 files로 재계산했다. 공개 기능·기여자
  기준선 2,214 commits·262 PR provenance·사람 20명은 유지되며, 이후 22 commits는 릴리스 준비·기록과
  #6588/#6594 정정뿐이라 새 제품 기능·기여자 유입이 없다.
- [PR #6592 검토 기록](../pr/archives/pr_6592_review.md)의 최종 판정은 `승인`이다. 다음은 이 review-only
  기록을 local commit한 뒤 별도 승인으로 `devel` push와 PR 본문 현행화를 수행하고, latest-head fast-pass와
  `MERGEABLE / CLEAN`을 확인한 뒤 정상 merge commit 승인을 받는 절차다.

## jeong-sik PR #6595--#6631 통합 후보

- 기준: `upstream/devel` `51043f5f8d0453b9bc929233de443fa60cb3df4b` 위 `review/jeong-sik-batch-20260902`.
- provenance-preserving cherry-pick: #6595 `a0596bf0`, #6602 `90bcefeb`, #6607 `92ee899b`, #6609 `a84c90ab`, #6618 `7351fe3e`, #6620 `a3b2bee6`, #6626 `1a68d9dc`, #6629 `965297c2`, #6631 `b5736a15`. #6605 `c2271acd`는 #6595와 patch-id가 같아 별도 적용하지 않았다.
- 메인터너 보정: native HWP5 1x1 wrapper가 padding 뒤에도 선언 height를 보존하도록 scoped layout 보정했다. single-page `export-svg`의 무접미 SVG를 fidelity compare가 누락하던 false-negative도 회귀 3건으로 보정했다.
- 검증: full nextest `8951 passed, 46 skipped`; fmt, host/WASM clippy, workspace build, manifest/tier, `scripts.tests.test_fidelity_compare` `54/54`, native `rhwp-wasm-build`, `git diff --check`를 통과했다.
- 시각 증적: 18개의 stable PNG와 SHA-256 manifest를 `mydocs/pr/assets/pr_6595_6631_jeong_sik_integration_20260902/`에 고정했다. #6620 WMF `0.12%`, #6626 차트 5종 `1.08--1.84%`, #6607 draw-group `0.91%`; #6629/#6631은 전체 text shaping 차이를 대상 좌표 판정과 분리했다.
- 판정: #6595, #6602, #6607, #6609, #6618, #6620, #6626, #6631 승인; #6605 머지 보류(중복); #6629 메인터너 보정 됨 수용 가능.
- 다음: review·증적 trailing commit을 local로 고정할 수 있다. upstream temporary branch push와 integration PR 생성은 별도 승인 후에만 수행한다.

## #6641 필드 편집 후 소유 문단 재조판

- #6628 Stage 4 정상 입력 전수에서 `batch-ops/BO05`, `BO15`만 정상 필드 채움 뒤 저장 왕복
  불일치로 실패한 제품 원인을 별도 이슈 #6641로 분리했다.
- 제품 WIP는 `task_m100_6641`의 `ce2fb30b8`에 보존했다. 변경은
  `src/document_core/queries/field_query.rs`와 `tests/batch_fill_contract.rs` 두 파일뿐이다.
- WIP 격리 빌드로 BO05·BO15가 모두 통과해 원인 축은 확인했지만, 본문 HWP5 외 표 셀·글상자·
  다단 중첩 경로의 직접 회귀 계약은 아직 없다.
- 최신 `upstream/devel@2edbe62e5`은 WIP 부모보다 16커밋 앞서며 renderer·table/paragraph layout
  변경을 포함한다. 수행계획 승인 뒤 정상 merge했고, 충돌은 같은 날짜 오늘할일에 양쪽이 후행 기록을
  추가한 한 건뿐이어서 jeong-sik 통합 기록과 #6641 기록을 모두 보존해 해소했다.
- `mydocs/plans/task_m100_6641.md`에 최신 devel 보존 merge, 지원 행렬 red 계약, 소유자 인식 reflow,
  기존 source 회귀, Rust 필수 검증, #6628 exact candidate 인계 순서를 작성하고 승인을 받았다.
- 현재 상태: 최신 devel merge 뒤 Stage 1 지원 행렬과 focused 계약을 확인한다. 원격 mutation·push와
  장시간 전체 검증은 수행하지 않았다.
- 2026-09-03 Stage 1~3: 가상 셀 fieldId의 by-id 쓰기 비대칭을 복구하고, 본문·표 셀·글상자·
  깊은 HWPX owner reflow의 값·LineSeg·char metadata 계약을 기존 integration source에 고정했다.
  `batch fill --verify`를 포함한 focused 56/56이 통과했고 #2724 stale exemption 한 건을 제거했다.
- HWPX 합성 LineSeg 저장 생략과 strict edit verify 사이의 일반 정규화 경계는 #6641에서 예외로
  숨기지 않고 별도 후속 후보로 분리했다. 현재 로컬 Stage 0~3 완료, Stage 4 장시간 Rust 게이트와
  원격 변경은 별도 승인 대기다.
- 원격 devel이 `8d4c25d01`로 20커밋 전진해 Stage 4 전에 정상 merge했다. 제품 충돌은 없고 오늘할일
  후행 기록 한 건만 양쪽 보존으로 해소했으며 merge commit은 `17dbbef65`다. 새 renderer 기준으로
  focused 56/56과 manifest 1129 sources·4821 attrs·48/48 targets를 다시 확인했다.
- Stage 4: exact source head `beaa64c5e`에서 AGENTS.md의 native/WASM/workspace lint 묶음과
  전체 release-test nextest를 순차 실행했다. 전체 `8,969/8,969 PASS`, 정책 skip 46건이며
  manifest check와 clean worktree까지 확인했다. 다음은 별도 승인 뒤 같은 candidate 계보로
  #6628 BO05·BO15 canary와 Gym 전수를 수행하는 Stage 5다.
- Stage 5: #6641 binary SHA-256 `6ea6d8ef99f86d93c5e0a2a9ba41509eb01d8c5929f4906dbc58008d2ce135f3`를
  #6628 runner `57b2e51a8`에 주입했다. BO05·BO15 canary 2/2, positive 1,035/1,035,
  discrimination 1,035 task·1,511 control·false-pass 0, trajectory 239/239 load-bearing가
  모두 통과했다. 임시 worktree와 제출물은 제거했고 저장소에는 Gym 자료를 변경하지 않았다.
- #6641 로컬 Stage 0~5는 완료됐다. 다음은 최종 결과 승인 뒤 latest source branch의 원격 push와
  PR 준비이며, #6628 branch 동기화·Stage 4 완료 반영은 #6641 병합 뒤 별도 절차다.
## planet6897 PR #6586--#6649 통합 후보 검토 (trailing record)

- 검토자 `@jangster77`을 13개 원 PR(#6586, #6589, #6604, #6610, #6613, #6615, #6616, #6622, #6625, #6627, #6633, #6636, #6649)에 검토 시작 전 지정하고 API로 확인했다.
- 최신 `upstream/devel` `2edbe62e5dc74db58c33df2c006ae587f86a1a71` 위 로컬 `review/planet6897-open-batch-20260902`에서 기능 커밋 18개를 `git cherry-pick -x`로 누적했다. 기능 집합 문서 기록 전 HEAD는 `208a18b8d7cd86568a3b1c15e026f202454631a9`다.
- #6622 head와 #6625 선행 `4c6e7ce`는 같은 patch-id여서 한 번만 적용했다. 원 PR을 직접 merge하거나 원격 branch를 변경하지 않았다.
- 검증: rust test-suite manifest prepare/check, rustfmt, workspace clippy, WASM clippy/build, workspace build, release-test nextest 전체 종료 코드 `0`, Native Skia lib `3,959`건, 모든 focused regression, `ir_field_sweep_baseline`, `text_overlap_baseline`을 통과했다.
- 직접 시각 증적과 한계는 [PR #6586--#6649 visual sweep](../pr/archives/pr_6586_6649_planet6897_visual_sweep.md)에 보존했다. #6627/#6633 원문은 현재 Mac의 한컴 계열 글꼴 부재로 text fallback이므로 구조·테두리 관찰까지만 기록했다.
- PR별 판정은 모두 `승인`이다. 메인터너 보정이 필요한 구현 결함은 발견되지 않았다.
- 다음 원격 단계: 이 문서·자산을 포함한 local trailing commit만 만들고, 별도의 명시 승인 전에는 upstream push, 통합 PR 생성, 리뷰 제출, comment, close, merge를 수행하지 않는다.

## M100 — v1.0.0 편집 기반

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| [#6042](https://github.com/edwardkim/rhwp/issues/6042) | 다중 페이지 scroll visibility·surface LRU·page scheduler | 완료 | Draft PR [#6637](https://github.com/edwardkim/rhwp/pull/6637) 생성, self-review 기록·로컬 검증 완료. stack restack·CI·Ready는 후속. 완료: 20:55 |
